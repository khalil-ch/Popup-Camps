/*const submit = document.getElementById("test");

submit.addEventListener('click', () => {
    const id = document.getElementById("id_f").value;
    const telephone = document.getElementById("telephone_f").value;
    const rib = document.getElementById("RIB_f").value;
   
    if ( false===(Number.isInteger(id_f)) || false===(Number.isInteger(telephone_f)) || false===(Number.isInteger(RIB_f)))
    {
        alert(' Prix et Quantite doit etre un nombre');
        }

})*/
<?php

/*require_once '../Model/orderRow.php';
require_once '../Controller/orderRowC.php';


// if the form's submit button is clicked, we need to process the form
	if (isset($_POST['submit'])){
        // && isset($_POST['qt']) && isset($_GET['id'])
		// get variables from the form
            $id = $_GET['id'];
            $qt = $_POST['qt'];
                		//write sql query
                   $order_row = new orderRow($qt,$_GET['id']);
                   $order_rowC = new orderRowC();
                   $categorieC->ajouterOrderRow($order_rowC);
        
                      echo "New Order Row created successfully.";
                     // header("location:viewCategorie.php");
                            
            }
        
*/
    ?>






    




